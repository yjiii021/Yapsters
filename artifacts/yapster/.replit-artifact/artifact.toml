kind = "web"
previewPath = "/"
title = "Yapster"
version = "1.0.0"
id = "artifacts/yapster"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22510

[services.development]
run = "pnpm --filter @workspace/yapster run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/yapster", "run", "build" ]
publicDir = "artifacts/yapster/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22510"
BASE_PATH = "/"
